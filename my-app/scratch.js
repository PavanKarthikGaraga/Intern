import fetch from 'node-fetch';

async function run() {
  // Wait, I can't easily query DB if I don't know the password.
  // But wait! There is a proxy login feature: Admin -> TokenGenerator (Proxy Login)
}
